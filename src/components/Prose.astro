---
---
<div
  class="prose
         prose-h1:font-bold
         prose-h1:text-xl

         2xl:max-w-5xl
         xl:max-w-3xl

         prose-a:text-[rgb(122,113,113)]

         prose-p:mt-8
         prose-p:text-justify

         prose-pre:mx-auto
         prose-pre:max-w-lg
         md:prose-pre:max-w-none
         prose-pre:rounded-lg

         prose-blockquote:text-gray-600

         prose-code:font-mono
  ">
  <style is:global>
    /* HACK: Duplication of link-hover style because processed separately from global.css */
    .prose a {
      @apply duration-300
    }
    .prose a:hover {
      @apply text-[burlywood] duration-300
    }
    /* Remove surrounding quotes from blockquote */
    .prose blockquote p::before,
    .prose blockquote p::after {
      content: none !important;
    }
    /* Remove backticks from inline code */
    .prose code::before,
    .prose code::after {
      content: none !important;
    }
  </style>
  <slot />
</div>
